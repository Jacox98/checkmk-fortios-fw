[package]
# Nome tecnico del pacchetto (usato come nome file .mkp)
name = fortigate_firmware

# Titolo leggibile che apparirà in WATO
title = FortiGate Firewall Firmware Monitoring

# Autore del pacchetto
author = Jacox98

# Breve descrizione (mostrata in WATO e nella lista pacchetti)
description = CheckMK extension for monitoring FortiGate firewalls via the REST API (firmware version, updates, and status).

# Versioni supportate
version.min_required = 2.3.0p1
version.packaged = 2.3.0p34
version.usable_until = 3.0.0

# Opzioni di validazione
validate_python = true
validate-mkp = true   # 🔹 Consiglio: aggiungi anche questa per validare la struttura del pacchetto

# Output build
output-dir = build

# Categoria (apparirà in WATO se definita)
category = Networking

# Licenza (buona pratica, mostrata agli utenti)
license = GPL-3.0

# Tag opzionali (utile se vuoi pubblicare su Checkmk Exchange o per chiarezza interna)
tags = fortigate, firewall, restapi, firmware
